
type Sign {
    _id: ID!
    boro: Borough!
    order_no: String!
    sequence: Int!
    distance: Int!
    arrowPt: String
    description: String
    code: String!
}

type Query {
    signs: [Sign]
}